# Generate your own secrets! (e.g. node -e "console.log(crypto.randomBytes(32).toString('hex'))")
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# LiveKit Cloud (https://cloud.livekit.io)
# For local dev with self-hosted LiveKit, use ws://localhost:7890
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Postgres credentials
POSTGRES_USER=migo
POSTGRES_PASSWORD=
POSTGRES_DB=migo
